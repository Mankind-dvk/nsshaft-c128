MEMORY {
    ZP:       file = "", start = $0002, size = $001A, type = rw;
    LOADADDR: file = %O, start = $1BFF, size = $0002;
    MAIN:     file = %O, start = $1C01, size = $37FF, fill = yes, fillval = $00;
}

SEGMENTS {
    LOADADDR: load = LOADADDR, type = ro;
    BASIC:    load = MAIN,     type = ro, start = $1C01;
    CODE:     load = MAIN,     type = rw, start = $1D00;
    CHARSET:  load = MAIN,     type = rw, start = $2800;
    MUSIC:    load = MAIN,     type = rw, start = $2A10;
    EFFECTS:  load = MAIN,     type = rw, start = $2E00;
    SPRITE:   load = MAIN,     type = rw, start = $3000;
    GAMEPLAY: load = MAIN,     type = rw, start = $3140;
    EXTCODE:  load = MAIN,     type = rw, start = $3800;
    PLAYERSETS: load = MAIN,   type = rw, start = $3A00;
    SELECTCODE: load = MAIN,   type = rw, start = $3CC0;
    GRAPHICS_CHARSET: load = MAIN, type = ro, start = $4000;
    TEXT_CHARSET: load = MAIN, type = ro, start = $4800;
    CHARSETCODE: load = MAIN,   type = ro, start = $5000;
}
