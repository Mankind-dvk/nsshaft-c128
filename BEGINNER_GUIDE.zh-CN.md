# NS-SHAFT C128 汇编代码阅读指南

这份文档面向已经知道“寄存器、内存、十六进制”这些基本概念，但还没有
独立写过完整 6502/8502 游戏的读者。它解释项目为什么这样组织，以及从
哪里开始读最容易。源码中的中文注释负责说明局部细节，本指南负责把各个
模块串成一条完整执行路径。

## 1. 先建立三个基本认识

### 1.1 这是原生 C128 程序

- CPU 使用 8502，但源码只使用标准 6502 指令集。
- 游戏运行在 C128 原生模式，不会切换到 C64 模式。
- 画面使用 VIC-IIe 40 列输出，而不是 VDC 80 列输出。
- 可见画面期间保持 1 MHz，避免 VIC-IIe 与 2 MHz CPU 访问冲突。

### 1.2 `main.s` 是唯一汇编入口

项目采用“单翻译单元 + 多 include 文件”的结构：

```text
main.s
  -> constants.inc
  -> macros.inc
  -> video.inc
  -> platform_materials.inc
  -> platforms.inc
  -> platform_rows.inc
  -> input.inc
  -> hud.inc
  -> score.inc
  -> health.inc
  -> character_select.inc
  -> player.inc
  -> fade_platforms.inc
  -> spring_platforms.inc
  -> conveyor_platforms.inc
  -> music.inc
  -> assets.inc
  -> state.inc
```

这些文件不是分别汇编后再链接的独立对象，而是由 ca65 按 `.include` 顺序
拼成一个对象文件。因此各模块可以直接引用其他模块的标签。

### 1.3 没有自动保存寄存器的函数调用约定

`JSR routine` 只会把返回地址放到硬件栈。例程内部对 A、X、Y 和状态标志的
修改不会自动恢复。除非注释明确说明，调用者应该假设这几个寄存器都会改变。
需要跨调用保存的数据放在 `state.inc` 的命名变量中。

## 2. 推荐阅读顺序

不要从 300 多行的音乐数据或字符位图开始。建议按以下顺序阅读：

1. `src/main.s`：看开机初始化、新游戏初始化和逐帧调用顺序。
2. `src/constants.inc`：遇到名字时回来查硬件地址和参数。
3. `src/state.inc`：理解程序实际保存了哪些运行状态。
4. `src/input.inc`、`src/hud.inc` 与 `src/score.inc`：输入、数字显示和定点
   滚动调度的短小完整链路。
5. `src/character_select.inc`：看 POTX 如何选角色、Fire 如何消抖，以及角色
   帧地址表怎样供标题、游戏和结束画面共用。
6. `src/platforms.inc`：学习 PRNG、数组地址表和 `(pointer),Y` 写屏幕。
7. `src/player.inc`：学习 9 位坐标、定点速度和碰撞状态机。
8. `src/video.inc`、`src/platform_rows.inc`：理解双缓冲、按平台描述绘制隐藏屏，
   以及自定义字符逐像素滚动。
9. `src/fade_platforms.inc`、`src/spring_platforms.inc`、
   `src/conveyor_platforms.inc`：理解平台生命周期和运行时字形复用。
10. `src/music.inc`：最后学习光栅 IRQ 和 SID。
11. `src/charset_ids.inc` / `src/charset_loader.inc`：字符编号与双源字库合成；
    `graphics_charset.inc` 只放自定义图形，`text_charset.inc` 引入文字源库。
    分配图见 [CHARSET_LAYOUT.md](CHARSET_LAYOUT.md)。Sprite 仍在 `src/assets.inc`。

## 3. ca65 与 6502 语法速查

### 3.1 数字和地址

```asm
lda #10          ; 十进制立即数 10
lda #$0a         ; 十六进制立即数 $0a，数值仍然是 10
lda #%00001010   ; 二进制立即数，同样是 10
lda $d020        ; 从地址 $d020 读取，不是立即数
```

`#` 表示“数值本身”。没有 `#` 时，操作数通常被当作内存地址。

### 3.2 标签的低字节和高字节

6502 一次只能装载 8 位。建立 16 位指针时要拆成两个字节：

```asm
lda #<SCREEN_A
sta ROW_PTR
lda #>SCREEN_A
sta ROW_PTR+1
```

`<` 取地址低八位，`>` 取地址高八位。

### 3.3 cheap-local label

以 `@` 开头的标签只属于它前面最近的普通标签：

```asm
routine_a:
@loop:
    ; ...

routine_b:
@loop:
    ; 这是另一个 @loop，不冲突
```

### 3.4 进位标志 C

进位标志既参与加减法，也经常充当布尔返回值：

```asm
clc              ; 加法前清进位
adc #40

sec              ; 减法前置进位，表示“不借位”
sbc #8

sec              ; 某些例程用 C=1 表示 true
rts
```

`BCC` 表示 C=0 时跳转，`BCS` 表示 C=1 时跳转。

## 4. 构建和装载发生了什么

运行：

```powershell
.\build.ps1
```

脚本分两步：

1. `ca65` 把 `src/main.s` 和全部 include 转成目标文件。
2. `ld65` 根据 `c128-prg.cfg` 把各 segment 放到固定地址并输出 PRG。

PRG 前两个字节是装载地址 `$1c01`。装载后，BASIC 看到一行：

```basic
10 SYS 7424
```

7424 的十六进制是 `$1d00`，也就是 `start`。

## 5. 当前内存地图

| 地址范围 | 内容 |
| --- | --- |
| `$0400-$07ff` | 屏幕缓冲 A 和 sprite 指针表 |
| `$0c00-$0fff` | 屏幕缓冲 B 和 sprite 指针表 |
| `$1c01-$1c0c` | BASIC 7 启动行 |
| `$1d00-$27d5` | 主循环、视频、HUD、计分、平台描述/绘制和状态 |
| `$2800-$2a0f` | 66 个自定义字符 |
| `$2a10-$2dd2` | SID 播放器和编曲数据 |
| `$2e00-$2f2c` | 消失平台效果代码 |
| `$3000-$30ff` | 玩家四张 sprite 帧 |
| `$3100-$313f` | 可写表针 sprite |
| `$3140-$3779` | 玩家物理、顶部受伤恢复、弹簧和传送平台代码 |
| `$3800-$39da` | 固定倒尖刺绘制、加权生成、HP 显示和尖刺恢复代码 |
| `$3a00-$3cbf` | Elien 受伤帧，以及 Ember/Wasser 的全部五帧 |
| `$3cc0-$3e64` | 选人流程、英文提示文本、角色帧地址表和颜色表 |
| `$4000-$47ff` | 第一源字库：仅自定义图形，2 KB |
| `$4800-$4fff` | 第二源字库：课堂示例大写字体，2 KB |
| `$5000-$5358` | 材质/碰撞查表、平台描述效果同步、传送带动画与字库合成 |
| `$d800-$dbff` | Color RAM |

这些固定地址不是随意选择的。VIC-IIe 的字符集和 sprite 块号都要求特定
对齐方式，链接脚本负责保证代码增长时不会悄悄覆盖图形数据。

## 6. 从 `start` 到 `main_loop`

### 6.1 `start`

`start` 只执行一次：

1. `SEI` 暂时关闭 IRQ。
2. MMU 映射 RAM bank 0，并确保 I/O 可见。
3. 强制 8502 使用 1 MHz。
4. CIA2 选择 VIC bank 0，也就是 `$0000-$3fff`。
5. 配置 VIC-IIe 字符模式、颜色和屏幕 A。
6. 初始化 paddle，清除旧 SID 状态。
7. 显示 Elien、Ember、Wasser 三个候选角色。
8. POTX 左/中/右三段高亮角色，第一次 Fire 锁定人选。
9. 锁定后显示 `PRESS FIRE WHEN CHARACTER FACES YOU`，并用
   left/normal/right 帧提示旋钮方向；只有回到 108..148、人物面向屏幕时，
   第二次 Fire 才开始游戏。朝左/右时误按会显示至少 12 帧 hurt，再要求重按。

### 6.2 `new_game`

`new_game` 每局都执行：

1. 关闭 sprite，重置翻页和 game-over 标志。
2. 关闭字符显示并合成游戏字库，清两张屏幕、Color RAM 和平台描述。
3. 清消失平台和弹簧状态，重置传送平台纹理与计数器，并安装其运行时字形。
4. 生成随机种子，清零分数/平台领取状态，再生成五个初始平台。
5. 画固定 UI、分数并初始化 POTX 和表盘。
6. 把玩家放到最低安全平台。
7. 初始化 SID 音乐。
8. 把屏幕 A 完整复制到屏幕 B，再开启字符显示。

### 6.3 `main_loop`

主循环以每个 PAL 帧更新一次为目标，调用顺序固定。若执行超时错过同步线，
会等到后续帧，不会自动补跑丢失的更新：

```text
等待帧边界
  -> 读取 POTX
  -> 更新五档表针和数字
  -> 水平移动
  -> 垂直物理/碰撞
  -> 首次落到未领取平台时加一分
  -> 消失平台计时
  -> 弹簧计时
  -> 左右传送平台纹理动画
  -> 选择玩家 sprite 帧
  -> 检查 game over
  -> 累加滚动相位；产生进位时推进平台一像素
```

把平台滚动放在物理之后很重要：人物先根据当前画面完成碰撞，再由平台和
“站在平台上的人物”一起上移一像素。

## 7. 屏幕矩阵、Color RAM 和混合字符模式

40 列文本屏幕是 40x25=1000 个屏幕码。某个坐标的地址为：

```text
screen_address = screen_base + row * 40 + column
```

当前关闭 ECM、开启多色字符模式；屏幕码直接选择字形，每格 Color RAM 决定显示方式：

```text
Color RAM bit 3=0：8x8 单色，1 位=1 像素
Color RAM bit 3=1：4x8 多色，2 位=1 个宽像素
多色位对 00=黑，01=白，10=灰，11=该格低三位指定的颜色
```

普通砖台阶和边框使用 Color RAM=10：bit 3 开启多色，低三位为红色。红砖、
白缝和黑色空白能同时出现在一个滚动碎片中。源图案的 8 行按原顺序分配到
UPPER/LOWER，纵向依然每次移动一像素。消失台阶使用独立的红砖灰缝字形，
对应完整、部分灰缝脱落、灰缝完全脱落三级外观。
传送平台使用 Color RAM=14，位对 01/11 显示白/蓝，空白碎片行用 00 显示黑色。
尖刺使用白/绿多色；文字、表盘和弹簧保持单色，以保留精细线条。

硬件可索引 256 个字形，但当前输出预留 528 字节，使用前 66 槽；
这现在是内存布局限制，不再是 ECM 限制。扩展需同时调整紧邻的音乐内存布局。

## 8. 为什么要双缓冲

如果一边显示一边逐字符改写同一屏幕，玩家会看到上半部分是新画面、下半
部分还是旧画面。项目准备两张矩阵：

- `active_screen` 指向当前可见矩阵。
- 转场先快速清空隐藏矩阵的游戏区，再根据平台描述绘制连续区间。
- 完成后写一次 `$d018` 切换矩阵；寄存器不会自动等到下一帧才生效。

隐藏屏仍保留上次使用的内容，所以只画新平台而不清旧内容会留下残影。
这里只清游戏区，不清固定框线、倒尖刺、HUD 或 sprite 指针。Color RAM 和动态
字库都只有一份，不能随 `$d018` 翻页；颜色/字形写入及翻屏仍需满足光栅时序。

## 9. 自定义字符如何实现逐像素上移

平台逻辑上仍位于字符行，但跨越字符边界时会拆成两部分：

- `UPPER`：已经进入上一字符行的像素。
- `LOWER`：仍留在原字符行的像素。

`fine_scroll` 从 7 减到 0：

1. 相位 7 开始时，根据平台描述在隐藏屏绘制 UPPER/LOWER 区间并翻屏。
2. 中间相位只重写这些动态字形的八条像素行。
3. 相位 0 完成时，把 `fine_scroll` 设回 7，平台描述与计分行一起上移。
4. 清隐藏游戏区并按描述画 FULL，在底部生成新逻辑行，再翻屏。

`platform_rows.inc` 用三个 20 字节数组记录每个逻辑行的平台起点、宽度和
FULL 字形编号；宽度 0 表示空行，FULL 编号同时表达类型与破裂/压缩状态。
当前关卡每行最多一个平台，平台间保留空行。`draw_platform` 绘制时登记描述，
消失/弹簧效果先更新描述再同步两屏，清场景时重置描述。这样转场不用扫描
540 格来反推平台位置；中间六个像素步骤仍只改动态字形。碰撞暂时继续读取
可见屏幕，不能只往矩阵写字符就把它当作可持久滚动的新平台。

这里没有修改 `$d011` 的滚动位，所以右侧 HUD 不会上下振动。

## 10. 平台随机生成

`random_state_low/high` 构成 16 位 Galois LFSR。`seed_random` 混合 KERNAL
时钟、CIA 定时器、TOD、当前光栅线和两个已加载程序内的字节。后两者不是
开机 RAM 熵；游戏接管 IRQ 后 KERNAL 时钟也不再更新。时序变化可改变种子，
但不保证每局唯一；复现需要相同种子、难度变化和 PRNG 调用顺序。
`random_byte` 每次连续推进八个 LFSR bit，再异或高低字节；不能只推进一位就
把结果当作新字节，否则相邻返回值会共享七位，平台类型会出现明显偏置。

每个平台依次消耗随机字节来决定：

1. 宽度 7..12 字符。
2. 合法水平起点。
3. 当前分数档的加权平台类型。

分数不仅调整滚动速度，也逐步开放平台种类并降低普通平台概率：

| 分数 | 可生成类型 | 普通平台概率 |
| ---: | --- | ---: |
| 0..4 | 普通、弹簧 | 75% |
| 5..9 | 普通、弹簧、消失 | 50% |
| 10..19 | 再加入左右传送 | 37.5% |
| 20..39 | 再加入尖刺，即全部类型 | 25% |
| 40+ | 全部类型，并提高尖刺/消失权重 | 12.5% |

这些池分别有 4 或 8 个元素，因此随机字节只需 `AND 3` 或 `AND 7` 就能得到
无偏索引。普通平台不限制连续次数；其余每一种最多连续两个。若候选会成为
第三个相同平台，代码会继续推进同一条 PRNG 序列并重选。只有最终实际绘制的
类型才写入 `last_generated_platform_type/generated_type_run_length`，所以被强制
改成普通类型的出生平台也不会污染连续记录。

六种生成类型，加上消失平台激活后的一个碰撞状态：

| 类型 | 碰撞返回值 | 行为 |
| --- | ---: | --- |
| 普通 | 1 | 安全支撑 |
| 尖刺 | 2 | 首次踩到先得 1 分；随后有 HP 时扣 1 并反弹，HP=0 时 game over |
| 未触发消失 | 3 | 激活倒计时并支撑 |
| 已触发消失 | 4 | 继续支撑直到删除 |
| 弹簧 | 5 | 压缩后发射 |
| 向左传送 | 6 | 正常支撑，每三帧把人物向左推 2 像素 |
| 向右传送 | 7 | 正常支撑，每三帧把人物向右推 2 像素 |

## 11. 玩家坐标为什么拆成多个字节

VIC sprite X 范围是 0..511，需要 9 位：

- `player_x_low` 写入 `$d000`。
- `player_x_high` 最低位写入 `$d010` bit 0。

表针是 sprite 4，它的第九位在 `$d010` bit 4。写玩家坐标时必须只修改
bit 0，不能整字节覆盖 `$d010`。

Y 坐标只需要 8 位。`player_y` 表示 sprite 顶端，脚底位置为：

```text
player_y + PLAYER_HEIGHT
```

## 12. 1/8 像素定点垂直速度

6502 没有浮点数。项目把速度单位定义为 1/8 像素：

- `player_fall_velocity=8` 表示每帧 1 像素。
- `player_fall_fraction` 保存不足 1 像素的累计余数。

下落时每帧增加速度，累计值达到 8 才让 Y 增加 1。弹簧上升初速度是 24，
即一开始可在一帧上移 3 像素；随后每帧减 1，直到速度归零再进入下落。

上升和下落分成两个无符号速度变量，比在 6502 上处理有符号负数更直观。

## 13. 碰撞检测

`find_platform_below_player` 做四步转换：

1. 用 `player_y + 21` 得到脚底 Y。
2. 减去当前字符矩阵顶部和软件滚动相位。
3. 右移三次，相当于除以 8，得到字符行。
4. 在 sprite 内部 X 偏移 5 和 18 的两个脚点采样屏幕码。

只在脚底距离平台顶部 0..2 像素时返回碰撞。这样人物不会从平台侧面或底面
吸附上去。滚动期间只把 `LOWER` 当作逻辑平台锚点；`UPPER` 只是视觉碎片。
字符行地址直接查 `playfield_row_low/high`，屏幕码再查
`collision_type_by_char` 得到 0..7 的逻辑类型，不再逐行累加地址或执行长比较链。

## 14. 消失平台与弹簧平台

### 14.1 消失平台

平台有三级外观，每级各有独立的 FULL/UPPER/LOWER 字形：

1. 未激活：完整红砖和灰色砂浆，使用 56/57/58。
2. 踩下后立即：部分灰色砂浆脱落，使用 7/16/17。
3. 经过 25 帧：灰色砂浆全部脱落，只剩红砖和黑色空隙，使用 59/60/61。

经过 50 帧后平台消失。倒计时由原来的 100 帧减半为 50 帧，PAL 下约从两秒
缩短为一秒。`fade_platform_timer` 从 50 递减，`fade_stage=0` 表示部分脱落；
计时到 25 时设为 `fade_stage=1`，切换到仅剩红砖。不再使用闪烁计数器或红灰交替。

使用三项表按 FULL/UPPER/LOWER 对应转换，不修改屏幕码高两位。
Color RAM 固定为 10，位对 00=黑色空隙、10=灰色砂浆（12）、11=红砖（2）；
共享位对 01 仍为白色，全局调色板不变。到期后只从两屏删除已激活字形，
不影响普通砖台阶；消失前的支撑和碰撞处理不变。

同时只跟踪一个已激活消失平台，这是用有限字符槽换取简单逻辑的设计取舍。

### 14.2 弹簧平台

人物着陆时：

1. 保存 `collision_row`。
2. 两张屏幕的 normal 弹簧码换成 compressed 弹簧码。
3. 等待 8 帧。
4. 恢复 normal 字形。
5. 清除支撑并设置向上初速度 24。

若人物在 8 帧内横向走出平台，`cancel_spring_platform` 只恢复图形，不发射。

### 14.3 左右传送平台

传送平台重复显示白蓝纹理，随世界上移的同时，纹理持续向左或向右滚动。
平台的矩形边界不横移。人物脚底碰撞返回 6/7 后，
垂直状态机继续保留支撑，并在本帧 paddle 移动之后按独立计数器每三帧推动
2 像素。它与 paddle 普通档平均速度相同：反向操控可以基本抵消，顺向操控
则会叠加速度；人物被推出平台边缘后，下一帧脚底采样为空并进入正常下落。

向右平台使用八字节源图 `$5f,$d7,$f5,$7d,$7d,$f5,$d7,$5f`；向左平台使用其
水平镜像 `$f5,$d7,$5f,$7d,$7d,$5f,$d7,$f5`。镜像只反转每行四个位对的排列，
不反转位对内部的颜色编码，因此白蓝不变。
Color RAM=14（8 | 6）开启多色并选择蓝色；每字节四个位对，01 为白色、11 为蓝色，
八行合起来是屏幕上 8x8 像素的纹理。`initialize_conveyor_platforms` 将源图
复制到各八字节的 `conveyor_left_pattern`、`conveyor_right_pattern`，并重置
`conveyor_animation_counter`。

主循环每帧在 `update_spring_platform` 后调用 `update_conveyor_animation`。
每三帧把纹理的每行循环左移/右移一个位对，即两个横向显示像素；四个相位
共十二帧循环。即使没有人物站上，或者本帧世界没有上移，动画也照常计时。
此计数器独立于人物的传送推动计数器，不改变原来的推人速度。

当前输出预留 66 个槽，传送平台仍采用节省字形的 FULL/LOWER 复用方式：

1. 每个方向让 FULL 字符在平滑滚动期间兼任 LOWER，只额外保留 UPPER。
2. FULL 分配在本体区 5/6，UPPER 分配在碎片区 18/19，合计四个槽。
   菜单不再复用任何碎片槽；进入菜单时装入完整的基础文字页，开始游戏时
   再合成“自定义图形 + HUD 所需文字”。两个源字库的内容始终不修改。
3. 横向动画每次更新都按当前纵向相位重建完整字形或上下碎片。粗滚动完成时
   从当前运行时纹理恢复 FULL，不从原图恢复，因此不会重置横向动画相位。

## 15. Paddle 五档控制

| POTX | 表针状态 | 水平移动 |
| --- | --- | --- |
| 0..63 | 最左 | 每两帧 2 像素 |
| 64..107 | 左 | 每三帧 2 像素 |
| 108..148 | 中 | 停止 |
| 149..191 | 右 | 每三帧 2 像素 |
| 192..255 | 最右 | 每两帧 2 像素 |

“每三帧”和“每两帧”来自计数器重装值 2 和 1。计数器包含执行动作的当前
帧，所以重装 2 实际周期是当前帧加后续两帧。

## 16. 六位分数与滚动难度

分数表示首次落到的新平台数量。游戏区第 3..22 行的 20 个逻辑行各有一个计分状态：
0 表示没有平台，`$01` 表示尚未领取，`$81` 表示已经领取。字符完成一次粗
滚动时，这 20 个状态与平台一起向上移动；底部新平台重新写入 `$01`。

安全支撑平台从空中的 `@land` 分支调用 `award_platform_landing_score`；尖刺则
从专门的 `@fall_on_spikes` 分支调用同一例程。例程把 `collision_row` 换算成
游戏区索引，只有 `$01` 才加一并改成 `$81`。因此弹簧弹起后重新落回、走开
再回来，以及继续站在平台上都不会重复得分。出生平台一开始就是 `$81`，
离开后再落回也没有第一分。右栏分两行显示 `SCORE:` 和六位数字 `000000`。

尖刺是“不提供支撑但仍算到达”的特殊情况：下落碰撞得到类型 2 后，代码先
调用同一个 `award_platform_landing_score`，再进入扣 HP/反弹或死亡流程。
claimed 位会在伤害前设成 `$81`，所以即使反弹后再次接触也不会重复得分。

六位数没有保存成一个二进制整数，而是六个连续的 0..9 字节。加分从个位向
左进位，显示时直接加 `CHAR_DIGIT_0`，这样不用除法，也不用 `SED` 十进制
模式。后一点很重要：SID IRQ 随时可能打断主循环，避免 `SED` 就不会让音乐
中的 `ADC` 意外按 BCD 运算。

滚动速度也不是整数帧延时，而是 8 位相位累加：

```text
accumulator = accumulator + rate
如果产生进位：世界上移 1 像素
```

初始 `rate=128`，平均每帧 0.5 像素。分数到达 20、40、60、80 时，
依次使用 160、192、224 和“每帧一次”模式，对应初速的 1.25、1.5、1.75、
2 倍。前三档仍由 8 位相位累加器产生稳定平均速度；精确的 2 倍需要
256/256，但 256 无法写入一个字节，所以 `scroll_rate=0` 专门表示每帧直接
滚动一个像素。由于分数只由新平台首次落地增加，滚动变快不会自动制造更多
分数，不再存在“加速导致更快得分、再导致加速”的正反馈。同一分数还在
5、10、20、40 分更新第 10 节的平台生成档位。

同一速度档位也会更新 `music_tempo_increment`。音乐使用模 512 的 9 位相位：
基础增量 44，后四档是 55、66、77、88，因此正好是 1、1.25、1.5、1.75、
2 倍，不会因为 8 位整数舍入而偏快或偏慢。这里只缩短相邻音符事件的时间，
不会修改 SID 频率表，所以听起来是编曲加速而不是整体升调。五档约为
129、161、193、226、258 BPM。

### 16.1 HP 奖励与伤害

右栏第三组固定字符显示 `HP:000`。每次新平台得分只增加
`health_score_progress`；累计到 3 时 progress 清零、`health_points` 加一，
六位分数完全不变。HP 用二进制字节保存，显示变化时才循环减 100/10 转成
三个十进制数字，因此每帧没有额外除法负担。

普通尖刺平台和顶部固定倒尖刺都可以消耗 HP：

- 落到尖刺：扣 1 HP，清除旧支撑并赋予向上速度 16，立刻离开尖刺。
- 接触顶部倒尖刺：扣 1 HP，取消弹簧压缩、清除支撑与上升速度，将头顶移到
  尖端下方 8 像素，再恢复重力下落。不计分，也不向上反弹。
- 掉到底部：不检查 HP，仍然立即 game over。

消耗最后一个 HP 后，本次伤害仍被吸收，HUD 变成 `HP:000`；
下一次接触任一种尖刺才会结束游戏。脱离接触面避免同一次接触在连续帧里重复扣血。

砖纹顶框在第 1 行，倒尖刺固定在第 2 行的游戏区，滚动从第 3 行开始。
固定几何使用 sprite 坐标系的屏幕起点 Y=50，因此尖端在 Y=73；平台软件
滚动使用的 43 是带相位偏置的基值，不能直接拿来计算固定尖刺位置。
人物头顶接触尖端时调用 `handle_ceiling_damage`；托举像素步和弹簧逐像素上升
都会立即检查。恢复例程放在 `GAMEPLAY` 段，避免挤占紧邻 sprite 的 `EXTCODE`。
成功扣除 HP 时还会把 `player_hurt_timer` 重装为 16。选帧例程每帧先检查这个
计时器，因此约 0.32 秒内所选角色的 hurt 帧会覆盖下落、左右和 normal 帧。
Game Over 画面清屏后重新设置 sprite 0 指针，并把同一角色的受伤帧固定显示
在两行文字之间；这仍然只使用硬件 sprite 0。

## 17. 选人、Sprite 指针和五档表针

屏幕末尾的 sprite 指针保存“地址除以 64”的块号。例如玩家 normal 帧位于
`$3000`，所以指针值是 `$3000 / 64 = $c0`。

三个角色各有 normal/fall/right/left/hurt 五帧，共十五个常驻内存块。
Elien 的四张运动帧在 `$3000-$30ff`，其受伤帧和 Ember/Wasser 的十帧在
`$3a00-$3cbf`。`selected_character` 只保存 0、1、2；选帧代码用它索引五张
块号表，游戏中仍然只让硬件 sprite 0 指向其中一帧。

标题选人阶段临时启用硬件 sprite 0..2 显示三张 normal 帧。POTX 的 0..84、
85..170、171..255 三段分别选择 Elien、Ember、Wasser，未选中者显示灰色。
第一次 Fire 后只保留 sprite 0；POTX 小于 108 显示 left，大于 148 显示
right，中间显示 normal。第二次 Fire 还要完整经过按下和松开，且松开时仍在
中间区，避免一次长按穿过两个阶段或带着移动输入开局。

回中阶段同时显示 `PRESS FIRE WHEN CHARACTER FACES YOU`。菜单入口调用
`load_text_charset` 装入完整基础文字页，A-Z 和常用符号均可用；游戏入口
调用 `load_game_charset` 合成图形与 HUD 字体，不再临时恢复个别缺失字母。
若人物仍朝左/右时按 Fire，sprite 0 切到所选角色的 hurt
块至少 12 帧；松开后才按最新 POTX 恢复朝向，因此错误操作有明确视觉反馈。

表针只有一个可写 sprite 块，因此档位变化时从五个模板中复制 63 字节。
表盘本身是字符，不占硬件 sprite。

## 18. SID 光栅 IRQ

音乐不能依赖主循环是否在某帧完成了昂贵的屏幕转场。因此 VIC-IIe 在光栅
线 240 触发 IRQ，主循环在线 250 同步。

IRQ 流程：

1. 向 `$d019` 写 1 确认中断。
2. 调用 `play_music_frame`；其模 512 相位增量由当前分数速度档位决定。
3. 跳到 C128 KERNAL 的 `$ff33` 恢复寄存器和 MMU 状态。

不要把音乐 IRQ 和主循环同步放在同一光栅线，否则 IRQ 返回时主循环可能
已经错过该行，再多等待一整帧。

## 19. 修改代码时的安全检查

每次修改后至少执行：

```powershell
.\build.ps1
```

然后查看 `build/nsshaft-c128.map`：

- `CODE` 必须结束在 `$2800` 之前。
- `CHARSET` 必须正好是 `$2800-$2a0f`。
- `MUSIC` 和 `EFFECTS` 不能覆盖 `$3000` sprite 区。
- `GAMEPLAY` 必须结束在链接脚本保留区域内。
- `EXTCODE` 必须在 `$3a00` 前结束。
- `PLAYERSETS` 必须正好占据 `$3a00-$3cbf`。
- `SELECTCODE` 必须从 `$3cc0` 开始并在 VIC bank 0 的 `$4000` 前结束。

常见故障定位：

| 现象 | 优先检查 |
| --- | --- |
| 字符乱码 | `$d018`、字符集地址、屏幕码是否小于 66、Color RAM 模式位 |
| 人物翻页闪烁 | SCREEN_A/B 两张 sprite 指针是否同步 |
| HUD 上下振动 | 是否误把 `fine_scroll` 写入 `$d011` |
| 平台提前 8 像素碰撞 | 是否错误地把 `UPPER` 当成逻辑支撑 |
| 表针消失 | 模板是否正好 63 字节、sprite 4 指针是否为 `$3100/64` |
| 无声音 | `$d01a/$d019` IRQ 配置、SID 音量、VICE Sound 设置 |
| 第二局状态异常 | 新变量是否在 `new_game` 所属初始化例程中清零 |
| 菜单字母变成平台碎片 | 是否调用 `load_text_charset`；是否误用游戏 `CHAR_*` 而非菜单 `TEXT_*` |

## 20. 适合初学者的练习

1. 改 `COLOR_YELLOW` 为其他颜色，观察玩家 sprite 颜色寄存器的作用。
2. 改 `PLATFORM_MIN_WIDTH`，确认随机起点仍不会越过分隔条。
3. 改 `SPRING_COMPRESSION_FRAMES`，观察图形压缩时间。
4. 改 `POTX_FAR_LEFT_END/POTX_FAR_RIGHT_START`，调整快速档范围。
5. 在 VICE monitor 观察 `player_y`、`player_rise_velocity` 和
   `player_fall_velocity`，理解弹簧状态转换。

练习时一次只改一个常量，并保留构建前后的 PRG。若行为异常，先恢复该变量，
不要同时改动滚动、碰撞和内存布局。

## 21. 如何阅读宏（Macro）

`macros.inc` 不是新的游戏子系统，而是汇编期工具层。宏名使用大写，
子程序仍使用小写：看到 `SET_WORD` 就去看宏展开，看到 `jsr update_player_vertical`
就去看被调用的子程序。不要写 `jsr SET_WORD`。

例如：

```asm
SET_WORD ROW_PTR, SCREEN_A
```

汇编时原地展开为：

```asm
lda #<SCREEN_A
sta ROW_PTR
lda #>SCREEN_A
sta ROW_PTR+1
```

`SCREEN_A` 是编译期地址，不是从该地址读取数据。低字节先写，高字节后写；
执行完 A 留下高字节，N/Z 也会变化，X/Y 与进位 C 不变。写 IRQ 向量时仍要由
调用者显式 `sei`，因为两次写入不是原子的。宏不会偷偷保存寄存器或关中断。

### 本项目的七个宏

| 宏 | 用途 | 寄存器与标志副作用 |
| --- | --- | --- |
| `SET_WORD destination, value` | 装载16位常量/地址 | 修改 A、N/Z |
| `SHOW_SCREEN layout` | 写完整 D018 布局 | 修改 A、N/Z；不更新 active_screen |
| `STORE_SCREEN_PAIR offset [, x/y]` | 把 A 写入两屏相同偏移 | 寄存器和标志全部不变 |
| `STORE_SPRITE_POINTER slot [, x/y]` | 把 A 写入两屏 Sprite 指针表 | 寄存器和标志全部不变 |
| `SET_REGISTER_BITS target, bits` | 读回后置位指定位 | 修改 A、N/Z |
| `CLEAR_REGISTER_BITS target, bits` | 读回后清除指定位 | 修改 A、N/Z |
| `ACK_VIC_RASTER_IRQ` | 向 D019 写1确认光栅中断 | A=1，N=0，Z=0 |

表格中的方括号表示可选参数，不需要真的输入方括号。例如：

```asm
lda #CHAR_P
STORE_SCREEN_PAIR SCORE_DISPLAY_OFFSET

lda potx_label, x
STORE_SCREEN_PAIR POTX_DISPLAY_OFFSET, x

lda #PADDLE_POINTER_BLOCK
STORE_SPRITE_POINTER 4
```

双屏写入不会修改 Color RAM；它只有一份，仍在调用处单独写。Sprite 宏中的
A 是64字节块编号而非图像地址，固定槽号必须为0..7；使用索引时还必须保证
运行期的槽号加索引不超过7。`SHOW_SCREEN` 也不会选择新的16KB VIC bank。

### 为什么不把所有代码都换成宏

- 宏每调用一次都复制一次机器码；本次替换的是原本就内联的指令，所以没有增大程序。
- 碰撞、计分、HP、随机平台、音乐播放仍由子程序负责，主循环调度次序没有改变。
- `CLEAR_REGISTER_BITS` 接收要清除的位：清 bit 0 应传 `%00000001`，不是 `%11111110`。
- 通用位操作不能用于 D019、CIA 中断状态等有副作用的寄存器；D011 读写高位语义
  不同，也保留显式代码。不要为了统一外观而隐藏硬件特殊性。
- C128 KERNAL 已经为当前 IRQ 入口保存寄存器和 MMU。本项目不添加 `PUSH_AXY` /
  `POP_AXY`，也不把末尾的 KERNAL 返回跳转改成 `rti`。

### 如何调试

先阅读宏定义的输入、输出和副作用，再查看 `build/nsshaft-c128.lst` 中的展开
机器码；VICE 仍然执行真实的 LDA/STA 等指令，不认识宏名。定义里的 `.ifblank`
和 `.assert` 都在汇编期处理，不是 CPU 运行时分支。

本次重构要求重构前后的 PRG 逐字节相同，而不仅是编译成功。这样可以确认没有
改变指令、地址、分支距离和执行周期；宏化提升的是源码可读性，不是游戏速度。
